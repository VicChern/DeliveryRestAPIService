image: maven:3.6.1

stages:
  - test

before_script:
  - export KekDomain=domain
  - export KekId=id
  - export KekWord=word


maven_build:
  stage: test
  script:
    - mvn verify -B
#  only:
#    - merge_requests
#    - master
#    - develop
